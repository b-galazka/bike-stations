import { DistanceUnitTranslationKeyPipe } from './distance-unit-translation-key.pipe';

describe('DistanceUnitTranslationKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceUnitTranslationKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
